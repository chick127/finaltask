# TODO for Login Flow Update

- [x] Modify SignInButton.tsx to include separate Google and GitHub sign-in buttons with callbackUrl to '/'
- [x] Update SignInButton.tsx to add callbackUrl to signOut for homepage redirect
- [x] Update Navbar.tsx to conditionally show menu links only when logged in
- [x] Test the changes to ensure popups close and redirect to homepage after login, logout redirects to homepage, and navbar menus are hidden when not logged in
